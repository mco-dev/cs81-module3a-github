// Greet a user by name
function greet(name) {
  return "Hello, " + name + "!";
}

// Returns even or odd depending on the number provided.
function oddOrEven(n) {
    return (n % 2 === 0) ? "Even" : "Odd";  //Shorthand if then else statment, check if dividing by two has a remainder.
}